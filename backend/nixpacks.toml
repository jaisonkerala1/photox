[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_8_2']

[phases.install]
cmds = ['npm ci --omit=dev || npm install --production=false']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'node server.js'

